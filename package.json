{
  "name": "nexus-games",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Nexus Games arcade — vanilla JS frontend, serverless API on Vercel",
  "scripts": {
    "migrate": "node scripts/migrate.js",
    "build": "node scripts/generate-posthog-config.cjs && node scripts/generate-sourcemaps.cjs && node scripts/migrate.js",
    "dev": "node scripts/dev-server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@better-auth/passkey": "^1.6.8",
    "@neondatabase/serverless": "^1.0.2",
    "better-auth": "^1.6.8",
    "resend": "^6.12.2"
  },
  "devDependencies": {
    "@vercel/node": "^5.0.0",
    "vitest": "^4.1.6"
  }
}
